YNAB_ACCESS_TOKEN=     # Personal access token from YNAB settings
YNAB_BUDGET_ID=        # Optional override (default: from config YAML)
ALERT_EMAIL_TO=        # Where to send alerts
ALERT_EMAIL_FROM=      # Sender address
SMTP_ADDRESS=          # e.g. smtp.gmail.com
SMTP_PORT=             # e.g. 587
SMTP_USERNAME=
SMTP_PASSWORD=
YNAB_CONFIG=           # JSON config (production only, replaces local YAML)
